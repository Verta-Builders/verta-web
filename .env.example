# Email Configuration (Resend)
# Get your API key from https://resend.com
RESEND_API_KEY=re_xxxxxxxxxxxxx

# Optional: Override the destination email (defaults to info@verta.builders)
# NOTIFICATION_EMAIL=your-email@example.com

# Optional: Override the sender email (must be verified in Resend dashboard)
# FROM_EMAIL=noreply@verta.builders
